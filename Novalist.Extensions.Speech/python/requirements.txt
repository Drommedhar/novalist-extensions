# Qwen's official package contains both the VoiceDesign and Base clone APIs.
# Pin the adapter-facing API: an unbounded model package can change method
# signatures or sampling defaults underneath an installed extension.
qwen-tts==0.1.1

# qwen-tts already depends on librosa, soundfile, torch and torchaudio. The
# environment builder replaces PyPI's CPU torch with the matching CUDA wheel on
# NVIDIA machines after this file is installed.
